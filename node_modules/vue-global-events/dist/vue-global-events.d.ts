import type { VNodeProps } from 'vue';

declare type EventFilter = (event: Event, listener: EventListener, name: string) => any;

/**
 * Component of vue-lib.
 */
export declare const GlobalEvents: new () => {
    $props: VNodeProps & GlobalEventsProps;
};

export declare interface GlobalEventsProps {
    target?: Exclude<keyof Window, number>;
    filter?: EventFilter;
}

export { }
